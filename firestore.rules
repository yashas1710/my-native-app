rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for testing (remove this rule in production)
    match /{document=**} {
      allow read, write;
    }
  }
}